import { describe, it } from "vitest";

describe.skip("schema-v2 performance placeholders", () => {
  it("keeps file registered until real performance cases are added", () => {
    // placeholder
  });
});
