# Frontend Constitution

## 1. 目的与适用范围
本文件是 `cms-vue3` 前端团队的最高开发约束，适用于仓库内所有前端代码、测试、样式、构建配置与文档变更。若其他说明与本文件冲突，以本文件为准；如需例外，必须在 PR 中说明原因、影响和回滚方案。

## 2. 核心原则
1. 可维护性优先于短期速度。
2. 一致性优先于个人偏好。
3. 类型安全、测试可证、行为可回归是合并前提。
4. 公共能力优先复用，禁止复制粘贴式扩散。
5. 任何 AI 或自动生成内容都必须经过人工审查。

## 3. 技术基线
- 包管理统一使用 `pnpm workspace`，禁止混用 `npm` / `yarn lock`。
- Vue 开发统一使用 Vue 3、Composition API、`<script setup>`、TypeScript。
- 新增状态管理、路由、构建或测试方案，必须先做团队评审。
- 不为局部便利引入与现有技术栈冲突的实现模式。

## 4. 代码组织与架构边界
- 页面、业务组件、基础组件、composables、stores、utils 必须职责清晰。
- 跨模块复用逻辑优先抽到共享层，禁止在多个包中复制实现。
- 禁止跨包引用私有内部文件，只能依赖公开导出接口。
- 单个文件过大、职责过多时必须拆分；复杂逻辑优先下沉到 composable 或 service。

## 5. 命名与编码规范
- 组件文件使用 `PascalCase`，如 `ArticleEditor.vue`。
- composable 使用 `useXxx`，store 使用清晰的领域命名。
- 工具函数与普通模块使用 `camelCase` 文件名；常量使用 `UPPER_SNAKE_CASE`。
- 统一 2 空格缩进，遵循仓库现有 ESLint、TypeScript 与格式约束。
- 禁止提交未使用代码、调试日志、注释掉的大段历史实现。

## 6. 组件与状态设计规则
- 展示组件与业务组件分离；组件对外 API 必须清晰、稳定、可预测。
- `props`、`emits`、暴露的数据结构必须显式声明类型。
- 能用局部状态解决的问题，不提升为全局状态；需要跨页面共享时再进入 store。
- 异步请求必须统一处理加载态、空态、错误态，禁止在页面中散落重复请求逻辑。

## 7. 测试与质量门禁
- 单元/组件测试使用 Vitest；端到端测试使用 Playwright。
- 改动 store、composable、utils、schema、关键交互或缺陷修复时，必须补充或更新测试。
- 合并前至少通过：`pnpm lint`、`pnpm typecheck`、与改动范围对应的测试。
- 未验证的重构、无法回归的行为变更、仅凭人工点测的核心逻辑，不得合并。

## 8. 性能、可访问性与安全
- 不引入无必要的大型依赖；新增依赖需说明收益与成本。
- 交互组件必须具备基本可访问性：可聚焦、可键盘操作、状态可感知。
- 禁止提交密钥、令牌、生产环境配置或敏感数据。
- 上传、鉴权、富文本、权限与外部请求相关改动，必须在 PR 中说明风险点。

## 9. Git 与 PR 规则
- Commit message 使用 Conventional Commits，例如 `feat:`、`fix:`、`refactor:`、`docs:`。
- 一个 PR 只解决一类问题，避免把功能、重构、格式化混在一起。
- PR 必须包含：变更目的、影响范围、验证命令、测试结果；UI 变更需附截图或录屏。
- 若存在破坏性变更，必须写明迁移方式和回滚方案。

## 10. 文档与决策记录
- 公共接口、共享能力、开发命令或环境变量变更后，必须同步更新文档。
- 重要架构决策、重大取舍和例外策略，应补充到 ADR 或设计说明中。
- 新成员应先阅读本文件、`AGENTS.md` 和相关包文档，再开始开发。

## 11. 执行与例外
- 本文件默认强制执行；代码评审应以此作为检查清单。
- 若业务紧急导致规则暂时放宽，必须在 PR 中记录例外原因、失效期限和补偿计划。
- 连续出现同类例外时，应修订流程或基础设施，而不是长期依赖人为兜底。
